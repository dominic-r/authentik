---
title: Dynamic in-memory stage
description: A temporary stage injected at runtime to alter the current flow execution without persisting configuration.
---

This term refers to a stage that is appended dynamically to a running flow without being saved in the database. It lets authentik adjust behavior for the current session only.

